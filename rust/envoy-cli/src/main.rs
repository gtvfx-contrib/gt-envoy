//! Native `envoy` CLI dispatcher entry point.
//!
//! Placeholder for the Phase 4 port of `py/envoy/_cli.py`. Once ported,
//! this binary replaces the PyInstaller-built `dist/envoy.exe` and links
//! `envoy-core` directly with no Python runtime dependency.

fn main() {
    println!("envoy-cli: scaffolding placeholder (Phase 4 will port py/envoy/_cli.py here)");
    std::process::exit(0);
}
